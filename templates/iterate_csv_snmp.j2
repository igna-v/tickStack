telegraf_plugins_snmp:
{% for item in csvfilesnmp.split("\n") %}
{%   if loop.index != 1 %}
{%     set list = item.split(",") %}
  - plugin: snmp
    modelo: "{{ list[0]|trim() }}"
    url: "{{ list[1]|trim() }}"
    community: "{{ list[2]|trim() }}"

{%   endif %}
{% endfor %}

